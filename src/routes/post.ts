import {Router} from "express"

const router = Router()

// Get ALl
router.get("/", )

// Get by ID
router.get("/:id", )

router.post("/")

router.put("/:id")

router.delete('/:id')

export default router

